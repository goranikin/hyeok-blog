export type LogbookEntry = {
  id: string;
  title: string;
  description: string;
  updatedAt: string;
};

export type TopicSlot = {
  top: string;
  left: string;
  rotate: string;
  accent: string;
  driftX: string;
  driftY: string;
  duration: string;
  delay: string;
};
